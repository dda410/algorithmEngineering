# Algorithm Engineering Template for Java

Included are the required scripts `run.sh` and `prepare.sh`.

Also included is a script (`create_archive.sh`) to create the zip archive in the right format.
